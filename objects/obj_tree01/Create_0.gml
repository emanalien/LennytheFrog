/// @description ???

// old code for creating a basic collider around the tree. Useless
// instantiate the collider
//this_collider = instance_create_layer(x,y, "Walls", Wall);
// set the height to be the same as current sprite.
//this_collider.image_yscale = (sprite_height / this_collider.sprite_height);